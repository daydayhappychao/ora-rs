const ora = require("ora");
const myOra = require("./index");

// const spinner = ora("Loading unicorns").start();
// spinner.text = "loading";

const o = new myOra.Ora({ text: "s" });

while (true) {}
// console.log(124);
